package task

func CreateTask(title, description string, status TaskStatus) (Task, error) {
	return Task{}, nil
}

func UpdateTask(t Task) (Task, error) {

}

func getTask(id int) (Task, error) {
	return Task{}, nil
}

func deleteTask(id int) (Task, error) {
	return Task{}, nil
}
